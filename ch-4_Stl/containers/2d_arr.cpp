// 2d array
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<vector<int> > arr(5, vector<int>(5, 0));
    // A vector of vectors

    // Each inner vector represents one row return 0;
}
