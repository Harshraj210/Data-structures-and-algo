#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;
int main() {
    vector<int> first;
    first.push_back(1);
    first.push_back(4);
    first.push_back(3);

    vector<int> second;
    second.push_back(1);
    second.push_back(4);
    second.push_back(3);
    // set union --> sb ko likh do
    

}